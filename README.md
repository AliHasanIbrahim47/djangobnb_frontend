## Getting Started

First, Add .env.local file in root folder:

NEXT_PUBLIC_API_HOST=http://localhost:8000
NEXT_PUBLIC_WS_HOST=ws://localhost:8000

Then, run the development server:

```bash
npm install
npm run dev
```
